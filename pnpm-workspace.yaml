packages:
  - 'packages/*'
  - 'apps/*'
  - 'services/*'
  - 'examples/*'
  # The Expo app installs standalone (its React Native deps would conflict with
  # the web packages' React 18 tree). Run it from apps/mobile, not the root.
  - '!apps/mobile'
