{
  "name": "@layera-labs/orbit-auth",
  "version": "0.0.1",
  "private": true,
  "description": "Pluggable end-user auth — verify a bearer token to an endUserId, via self-hosted (JWT) or Clerk / Supabase / Firebase",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run --config ../../vitest.config.ts --passWithNoTests"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vite-plugin-dts": "^3.8.0"
  },
  "sideEffects": false,
  "//private": "Server-side token verification for the render service. Not something an SDK consumer mounts, and its stores are in-memory by default."
}
